import { ShowToggleDirective } from './show-toggle.directive';

describe('ShowToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowToggleDirective();
    expect(directive).toBeTruthy();
  });
});
